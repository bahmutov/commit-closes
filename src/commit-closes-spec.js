'use strict'


/* global describe, it */
const commitCloses = require('.')

describe('commit-closes', () => {
  it('write this test', () => {
    console.assert(commitCloses, 'should export something')
  })
})
